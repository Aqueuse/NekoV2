package settings;

import javax.swing.*;

public class ListWithPreviewJPanel extends JPanel {
    public ListWithPreviewJPanel() {
        String[] toysList = {"Wool"};
        JList<String> toysJList = new JList<>(toysList);
        //this.setPreferredSize(new Dimension(100, 100));

        // function to refeel the petsList avec les assets filename dans ./images
        // puis refeed la JLIST

    }
}

